source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '2.6.3'

gem 'require_all'
gem 'client-api'
gem 'ffaker'

group :test do
  gem 'rspec'
  gem 'rspec_junit_formatter'
end

